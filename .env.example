# =============================================================================
# SOCIAL MEDIA AI SYSTEM - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local and update with your actual values
# Never commit .env.local files to version control!
# 
# Note: In Next.js, variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Only use NEXT_PUBLIC_ for non-sensitive data that's safe to expose publicly

# =============================================================================
# REQUIRED VARIABLES (Actually used in the codebase)
# =============================================================================

# Backend API URL - Used in apiClient.ts for all API calls
# For local backend: http://localhost:8000/api/v1
# For production backend: https://social-os-backend-5.onrender.com/api/v1
NEXT_PUBLIC_API_URL=https://social-os-backend-5.onrender.com/api/v1

# Node environment - Used in AppError.ts for development logging
# For local development: development
# For production deployment: production
NODE_ENV=development

# Gemini API Key - Used in next.config.mjs (server-side only)
# This is passed to server-side API routes if needed
GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# OPTIONAL VARIABLES (Not currently used but may be needed for future features)
# =============================================================================

# Supabase Configuration (if direct client-side access is needed)
# Currently all Supabase operations go through backend API
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-public-key

# CRON job secret (if implementing API route protection)
# CRON_SECRET=your-secure-cron-secret-key

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
# Your current deployment setup:
# Frontend: https://social-ms.vercel.app/ (Vercel)
# Backend:  https://social-os-backend-5.onrender.com (Render)
# 
# For production deployment on Vercel:
# 1. Set NEXT_PUBLIC_API_URL=https://social-os-backend-5.onrender.com/api/v1
# 2. Set NODE_ENV=production
# 3. Add your actual Gemini API key if using server-side AI features
# 
# For local development:
# 1. Set NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1 (if running backend locally)
# 2. Set NODE_ENV=development
